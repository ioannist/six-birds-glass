# F-II §16 checklist walk (G1 gate) — manager execution, 2026-07-06

Walked item-by-item against the finished draft (`paper/main.pdf`, 23 pp.; section numbers below
are the compiled paper's). Source of items: `design/reference/foundations_II.md` §16 (28 items,
A1–H3). Status vocabulary: PASS / PASS-with-note / N/A. Any FAIL would block; none remain.
Eddy's independent re-walk follows the full-draft review (G3).

## A. Domain and parameter hygiene

- **A1 PASS** — every continuous control parameter is a declared finite grid or Ann_Bridge:
  the ε grid {1/10..7/10} with T = −1/ln ε as bridge annotation (§3.1); t_w, τ ladders declared
  per artifact (§4.1, §8.1); no unbookkept continuum appears in any formal statement.
- **A2 PASS** — quantified classes are declared finite classes: lens catalog (hashes in every
  envelope), 512-predicate class (§6.1), 9-member benchmark family (§6.2), declared trap weight
  family (§3.2), protocol words over the ε grid (§3.1). Repair classes are never quantified over
  (GB5 not built — disclosed §11.2, gap register GAP-1).
- **A3 PASS** — all references resolve to declared records: evidence manifest + repo-wide
  citation-consistency tests; every prose claim carries a hash-pinned \claimref checked at build
  time (§2.2, §2.3); the GT6 promotion filing carries its full field list (§7).

## B. Role filing

- **B1 PASS-with-note** — no role name without threshold attachment: threshold data (margins,
  triage cutoffs, collapse cases) lives in the artifact payloads cited by each row; the prose
  never names a role projection without citing the artifact that carries its attachment.
- **B2 PASS** — GT1 filed exactly as channel status: "the P5 channel of the standard
  thermodynamic packaging is not active_projection as a closed packaging on this declared glassy
  scope" (§4.1); the inactive channel is never read as an active witness.
- **B3 PASS** — GT2's different-future files as P4 staged-dependence on the p1-witness template,
  explicitly NOT P3 route mismatch and NOT directionality (§5.2, verbatim).
- **B4 PASS** — no P3 content anywhere: the loop certificate was not built and the ledger's
  NEG row states no loop-asymmetry or P3 route-mismatch claim is made on GT2's behalf (§5.2).
- **B5 PASS** — no irreversibility/arrow/drive statement exists to audit; the standing nonclaim
  (CD > 0 and aging are not irreversibility) is stated in §5.2, §7.3, §11.3, and Table 7.

## C. Quantifiers and thresholds

- **C1 PASS** — the only "no X exists" claim (GT3) is scoped to the declared 512-predicate class
  with an exhaustive check and a constructive witness (§6.1); licensed shadow form used verbatim.
- **C2 PASS** — no strengthening beyond the declared repair class is asserted at any grade
  ("no admissible repair exists" never appears; §11.2, Table 7).
- **C3 PASS-with-note** — numeric criteria (margins 3/2 and 2, triage robustness cutoffs,
  GT7 interval bounds) are declared threshold data in the artifacts; error bars appear only as
  bridge/threshold annotations; none is treated as a new role or new state.
- **G3-adjacent note:** see G3 below for the recognition ceiling.

## D. Levels, lifts, promotions

- **D1 PASS** — every result carries exactly one grade as part of its statement (ledger-enforced;
  checker rule graded-row ⇒ G1 item); cross-level comparisons appear only inside the GT6
  promotion filing with its record (§7.1).
- **D2 PASS** — every added memory coordinate is a recorded, selected, non-unique lift; the
  buyback curve is published as a lift ledger (§5.3, verbatim).
- **D3 PASS** — GT6 carries the full promotion record + gate table + knockout panel (§7.2), with
  the checker-validated filing; the verdict basis is quoted verbatim (§7.1).
- **D4 PASS** — no lossless-forgetting or unique-lift claim; canonicity/uniqueness of M is
  attributed only to [A]'s generic theorems (verified generically in Lean, §9.3; narrative delta
  2); Dec-records' non-uniqueness is respected (lifts "selected and non-unique", §5.3).

## E. Visibility and shadows

- **E1 PASS-with-note** — visibility records: per-instrument records in all three bridge files
  (§10); model-side claims carry declared quantifier domains and suppressed content via their
  envelopes' nonclaims, printed in full (Table 7).
- **E2 PASS** — the field-facing sentence is published explicitly as the declared-class shadow,
  with class, catalog, and suppressed content named in the same sentence (§6.1); overreading
  declared inadmissible in the same paragraph.
- **E3 PASS-with-note** — GT4 controls are classified with raw and cleared statuses recorded
  (Table 3); the residual-scheme typing ((vi) instrument-visibility artifact, (iii) presentation
  variant, (viii) outside-domain) lives in the artifact taxonomy the table transcribes; no
  residual is left unstatused.

## F. Empirical legs

- **F1 PASS** — all three lab records carry the complete five-field bridge record, validated by
  the checker (§10).
- **F2 PASS** — no empirical-realization claim anywhere; "lab-grade witnesses via the bridge,
  full stop" (§10 + nonclaim).
- **F3 PASS** — visibility filed per instrument; no cross-instrument merge; the absence of a
  cross-instrument comparison theorem is stated (§10 nonclaim).

## G. Claim grades and conditional reporting

- **G1 PASS** — exactly one grade per result, in the statement, enforced mechanically
  (claims-ledger checker; §11.1 recapitulates the grade map).
- **G2 PASS** — conditional results name their hypotheses: GB1's open general theorem (§6.3),
  GT6's unexercised material-forcing route (§7.1), the GB5 quantifier discipline (§11.2).
- **G3 PASS (added during this walk)** — the recognition-open item (a) ceiling is now declared
  in §11.2: if glass aging carries a finite conservation/balance law over declared records
  supplying none of the six witness types, the decomposition is conditional on the recognition
  hypothesis; no candidate identified; any future one files at recognition grade separately.
  (This was the single genuine gap found by the walk; one paragraph added to the discussion.)
- **G4 PASS** — every Lean artifact carries its five-value-legend fidelity in the canonical
  table, printed in full (Table 6); no schema/trivial declaration described as verified; CD
  computations labeled deterministic computational evidence (§9.1).

## H. Overclaim guardrails

- **H1 PASS** — no restatement as a full algebra of glass roles, global independence, unique
  decomposition/state presentation beyond [A]'s proofs, empirical realization, or a claim about
  all glasses / all emergence / the continuum (§11.3; denylist test enforces the remediation
  phrases mechanically).
- **H2 PASS** — standing nonclaims printed in full: Table 7 (61 rows, unabridged), including
  CD-not-novelty/irreversibility, no ideal-glass-transition claim, no continuum limits,
  bridge-only empirics, no P3-as-directionality.
- **H3 PASS** — assembly order respected before the terminal claim: domain/shell (§2–3) →
  witnesses (§5) → foreclosure/classification (§6) → decomposition & gate table (§7) →
  integrated stress/nonclaim check (§11) → statement (§12). No skipping; the new layer is not
  self-certified (T24/T25 respected via the [C]-standard shell-local grade).

## Verdict

28/28 items PASS (5 with notes recording where the satisfying data lives; 1 — G3 — satisfied by
a paragraph added during this walk). G1 gate: **CLEAR**, pending Eddy's independent re-walk.

## Re-walk after the readability pass (manager, 2026-09-22)

Eddy's final review (gpt-6-astra, FIX-FIRST, 2026-09-22) reopened four items against the
readability-pass draft. Each was fixed in the prose and re-walked; the fixes are guarded by
`tests/test_paper_prose_bindings.py` and new denylist phrases in `tests/test_paper_gates.py`.

- **C1 re-PASS** — the "no X exists" claim is again confined to the declared 512-predicate class:
  the plain-language reframing ("any readout that separates ... must carry declared memory") was
  an overreach beyond the class and beyond what the exhibited separator (`n_1` under the identity
  continuation, a present structural coordinate) supports; replaced by the exact statement
  (§6.1). GT3 is no longer extended to the GT7 readouts (§8.3) or to "any description by present
  observables" (§7.1, §12).
- **D2 re-PASS** — the lift ledger wording is unchanged; the coordinate glosses now state the
  implemented semantics (`T_f(μ) = E_μ[i*(E(n))]`, an expectation of a grid index, not a
  temperature and not the TNM quantity), so no lift is described as more than what the code
  computes (§5.3; intro GT5 bullet; abstract).
- **E2 re-PASS** — the field-facing sentence is still published only as the declared-class
  shadow with class, catalog, and suppressed content named (§6.1, unchanged); the abstract's
  opening inference was softened so it no longer implies the shadow before the class is named.
- **H1 re-PASS** — the blanket-exactness sentences in the introduction ("every answer is an
  exact computation", "carry no numerical error") were removed in favor of the abstract's
  rational-vs-float64 separation (§1.3); the closure glossary no longer equates lumpability with
  packaging idempotence (exact counter-control: unconstrained N=2, ε=1/2, τ=1 has δ=2/9, now a
  regression test), and GB1's source theorem is described as an entropy-production audit, not a
  memory-exclusion theorem (§2.1, §6.3).

Also corrected in the same pass, outside the four reopened items: CD is reported in nats
(natural log, as computed), the Kovacs crossing is described as a randomized stop between the
bracketing steps 1 and 2 with exact mixing weight θ (§5.1–5.2), the GT4 fixtures are described as
schematic regime packages with constant-reset "honest" twins (§6.2), and the scoreboard caption
counts 21 interval + 1 order-relation scored FA predictions (Fig. 6).

Verdict after re-walk: 28/28 PASS. Pending Eddy's confirmation on the corrected draft.

**Eddy confirmation (round 3, gpt-6-astra medium, 2026-09-22): SHIP.** "The F-II §16 checklist's
28/28 PASS is now defensible, based on the cumulative review and this re-check of the reopened
items." One nonblocking editorial note (§1.2 "restore closure" → "recover the missing predictive
distinction") applied after the verdict. G1 + G3: **CLEAR**.

## 2026-09-23 — Citation-correctness round 1 (Eddy, gpt-5.6-sol medium, live search): FIX-FIRST → fixes applied

Eddy checked 72/72 external entries online and returned seven findings. Manager verification and disposition:

- **Accepted, reworded (§1.1):** "no static order parameter for glass has ever been found" → "no universally accepted static order parameter has emerged; conventional low-order static correlations show no clear signature, and the structural measures that do grow remain contested." The four reviews support the scoped claim, not the categorical one.
- **Accepted, reworded (§1.1):** "richer protocols defeat any single such variable" → "richer protocols expose the limits of any single such variable and have motivated multiparameter models." Ritland/KAHR/Hodge/McKenna–Simon support limits and multiparameter models, not a universal quantifier.
- **Accepted, reworded (§5.1):** Prados–Brey genericity now stated "in the linear-response regime", the hypothesis of their analysis.
- **Accepted, reworded (§9.2):** Chu-space biextensional collapse now "has the form of" / "analogous to", with an explicit sentence that no formal correspondence is claimed or proved.
- **Accepted (bib):** `bouchbinder-langer-2010` pages 3065 → 3065–3073 (confirmed via Semantic Scholar journal record; Crossref/OpenAlex carry only the first page).
- **Rejected (bib):** `parente-2024` author list. Eddy asked to add Mirna Džamonja. The arXiv record shows v1–v2 with both authors but v3 (26 Sep 2025, the version our DOI resolves to) single-authored, with the comment "Mirna Džamonja withdrew from authorship after the manuscript was accepted for publication." Adding her would contradict the author's own record. Kept Parente alone; the bib `note` now documents the version history so the credit trail is visible.
- **Provisionally accepted (bib):** `bertschinger-2006` pages 9–21 → 9–19. The IOS Press table of contents was unreachable from this sandbox (JS-rendered / bot-blocked). Secondary sources split: two citing arXiv papers (2010.01855, 1406.1502) and, per Eddy, the authors' Max Planck institutional listing give 9–19; two other citing papers (2311.10786, 1909.13045) and Semantic Scholar give 9–21. Went with the institutional listing; **Eddy round 2 is asked to confirm against a primary page.**

State after fixes: latexmk exit 0, 32 pages, 0 undefined citations; paper gates + prose bindings 26 passed; full suite rerun in progress.

## 2026-09-23 — Citation-correctness round 2 (Eddy, gpt-5.6-sol medium, live search): FIX-FIRST → two residuals fixed

Eddy agreed with six of seven dispositions, retracted its round-1 `parente-2024` authorship finding (v3 record confirms the coauthor withdrew after acceptance), and confirmed `bertschinger-2006` pages 9–19 against Olbrich's MPI MIS publication list (https://www.mis.mpg.de/de/people/eckehard-olbrich/publications). Two residuals:
- **Abstract** still carried the categorical "have not produced a static order parameter"; reworded to "have produced no universally accepted static order parameter", matching §1.1.
- **`parente-2024`** DOI was not rendered by `plainnat` for `@misc`; added a `howpublished` field carrying the arXiv id and `\doi{}` so the compiled entry is identifier-complete. The `doi` field is retained for the bib gate.

## 2026-09-23 — Citation-correctness round 3 (Eddy, gpt-5.6-sol medium, live search): SHIP

Both round-2 residuals confirmed fixed (abstract wording; `parente-2024` renders arXiv id and DOI, PDF link resolves). No new findings. Citation layer cleared. Final state: 81 bib entries (9 corpus + 72 external, all with DOI/ISBN), every key cited, latexmk exit 0 at 32 pages, 0 undefined citations, bibtex 0 warnings.
